# 🔐 Переменные окружения - Проверочный список

## Backend (`flerr-server`)

Откройте: Render Dashboard → `flerr-server` → Environment

### ✅ Обязательные переменные:

| Переменная | Значение | Где взять |
|------------|----------|-----------|
| `NODE_ENV` | `production` | Уже установлено |
| `MONGODB_URI` | `mongodb+srv://nastasy338_db_user:LYA32q3cclqzhzla@clusterfos5.vmmdblh.mongodb.net/flerr?retryWrites=true&w=majority&appName=ClusterFos5` | Ваша строка подключения |
| `JWT_SECRET` | (автогенерация) | Render генерирует |
| `JWT_REFRESH_SECRET` | (автогенерация) | Render генерирует |
| `APP_BASE_URL` | `https://flerr-web.onrender.com` | URL вашего frontend |
| `PORT` | `5000` | Уже установлено |

### 📧 Email переменные (опционально):
| Переменная | Значение |
|------------|----------|
| `SMTP_HOST` | `smtp.gmail.com` |
| `SMTP_PORT` | `587` |
| `SMTP_USER` | ваш email |
| `SMTP_PASS` | пароль приложения |

### 📦 S3/Backblaze переменные (опционально):
| Переменная | Значение |
|------------|----------|
| `S3_ENDPOINT` | `https://s3.us-east-005.backblazeb2.com` |
| `S3_REGION` | `us-east-1` |
| `S3_BUCKET` | имя бакета |
| `S3_ACCESS_KEY` | access key |
| `S3_SECRET_KEY` | secret key |

---

## Frontend (`flerr-web`)

Откройте: Render Dashboard → `flerr-web` → Environment

### ✅ Обязательные переменные:

| Переменная | Значение | Где взять |
|------------|----------|-----------|
| `VITE_API_URL` | `https://ваш-backend-url.onrender.com` | Render Dashboard → `flerr-server` (вверху страницы) |
| `VITE_IMGBB_API_KEY` | `ваш_api_ключ` | https://api.imgbb.com/ |

---

## 🔍 Как проверить Backend URL:

1. Откройте Render Dashboard
2. Выберите сервис **`flerr-server`**
3. Наверху страницы будет URL типа: `https://flerr-server-xxxx.onrender.com`
4. Скопируйте его полностью
5. Вставьте в `VITE_API_URL` для frontend

---

## ⚠️ Важно:

- После изменения ЛЮБОЙ переменной Render автоматически перезапускает сервис
- Это занимает 3-5 минут
- НЕ добавляйте backend переменные в frontend и наоборот!

---

## ✅ Быстрая проверка:

### Backend работает, если:
```
https://ваш-backend-url.onrender.com/health
```
Возвращает: `{"status":"ok"}`

### Frontend работает, если:
```
https://flerr-web.onrender.com
```
Открывается главная страница

---

**Если все переменные на месте и все работает - можно пользоваться платформой! 🎉**

