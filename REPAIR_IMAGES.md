# 🔧 Восстановление изображений (Repair Script)

## ✅ Что делает скрипт repair:

1. **Исправляет URL изображений в курсах:**
   - `ibb.co/...` → `i.ibb.co/...` (Direct link)
   - Оптимизирует Unsplash URL (добавляет параметры `?w=800&q=80&auto=format&fit=crop`)

2. **Восстанавливает изображения преподавателей:**
   - Обновляет фото для существующих преподавателей
   - Создает новых, если их нет

3. **Восстанавливает изображения галереи:**
   - Обновляет фото для существующих элементов
   - Создает новые, если их нет
   - Исправляет URL для всех остальных элементов

4. **НЕ удаляет данные:**
   - ✅ Все ваши курсы остаются
   - ✅ Все пользователи остаются
   - ✅ Все ваши добавленные элементы остаются

---

## 📋 Как использовать:

### Шаг 1: Запушить изменения

**GitHub Desktop:**

1. Вы увидите измененные файлы:
   - `apps/server/src/scripts/repair.ts` (новый скрипт)
   - `apps/server/package.json` (добавлена команда repair)

2. **Commit message:**
   ```
   Add: Image repair script to restore and fix images
   ```

3. **Commit to main → Push origin**

---

### Шаг 2: Дождаться деплоя backend

**Render автоматически задеплоит backend** (3-5 минут)

Дождитесь: `==> Build successful 🎉`

---

### Шаг 3: Запустить repair скрипт

1. **Render Dashboard** → `flerr-server`
2. Перейдите на вкладку **"Shell"**
3. Выполните команду:
   ```bash
   cd apps/server
   npm run repair
   ```

4. **Дождитесь завершения:**
   ```
   ✅ Repair completed successfully!
   📊 Summary:
      - Courses: X image(s) fixed
      - Teachers: X updated, X created
      - Gallery: X updated, X created, X other items fixed
   🎉 All images have been restored and fixed!
   ```

---

### Шаг 4: Проверить результат

1. **Обновите страницу** (`Ctrl + Shift + R`)
2. Проверьте:
   - ✅ Главная страница - изображение hero должно быть
   - ✅ Страница курсов - все обложки должны загружаться
   - ✅ Галерея - все изображения должны быть видны
   - ✅ Преподаватели - все фото должны быть видны

---

## 🔍 Что исправляется:

### URL изображений:

**Было:**
- `https://ibb.co/abc123` (страница ImgBB)
- `https://images.unsplash.com/photo-123` (без параметров)

**Стало:**
- `https://i.ibb.co/abc123.jpg` (Direct link)
- `https://images.unsplash.com/photo-123?w=800&q=80&auto=format&fit=crop` (оптимизировано)

---

## ⚠️ Важно:

- **Скрипт НЕ удаляет** ваши данные
- **Скрипт НЕ перезаписывает** ваши курсы
- **Скрипт только исправляет** URL изображений и восстанавливает стандартные изображения для преподавателей и галереи

---

## 🐛 Если что-то пошло не так:

1. **Проверьте логи** в Shell - там будет видно, что именно произошло
2. **Пришлите мне логи** - я помогу разобраться
3. **Можно запустить repair несколько раз** - он безопасен

---

## 📞 После repair:

Если изображения все еще не загружаются:

1. **Проверьте в админке:**
   - Откройте элемент галереи/курса
   - Посмотрите URL изображения
   - Если это `ibb.co/...` - замените на `i.ibb.co/...` вручную

2. **Или загрузите заново:**
   - Используйте кнопку "Загрузить фото" в админке
   - Это автоматически создаст правильный Direct link

---

**Удачи! После repair все изображения должны работать! 🎉**

